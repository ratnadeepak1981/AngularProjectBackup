import { AdminNotification } from './admin-notificationroutes';

describe('AdminNotification', () => {
  it('should create an instance', () => {
    expect(new AdminNotification()).toBeTruthy();
  });
});
