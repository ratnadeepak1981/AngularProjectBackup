import { AdminCertificate } from './admin-certificateroutes';

describe('AdminCertificate', () => {
  it('should create an instance', () => {
    expect(new AdminCertificate()).toBeTruthy();
  });
});
