import { FacultyManagement } from './faculty-managementroutes';

describe('FacultyManagement', () => {
  it('should create an instance', () => {
    expect(new FacultyManagement()).toBeTruthy();
  });
});
