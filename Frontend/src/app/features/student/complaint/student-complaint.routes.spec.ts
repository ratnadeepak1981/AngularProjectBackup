import { StudentComplaint } from './student-complaintroutes';

describe('StudentComplaint', () => {
  it('should create an instance', () => {
    expect(new StudentComplaint()).toBeTruthy();
  });
});
