import { StudentDashboard } from './student-dashboardroutes';

describe('StudentDashboard', () => {
  it('should create an instance', () => {
    expect(new StudentDashboard()).toBeTruthy();
  });
});
