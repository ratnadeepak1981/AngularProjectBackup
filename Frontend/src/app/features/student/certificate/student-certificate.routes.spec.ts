import { StudentCertificate } from './student-certificateroutes';

describe('StudentCertificate', () => {
  it('should create an instance', () => {
    expect(new StudentCertificate()).toBeTruthy();
  });
});
